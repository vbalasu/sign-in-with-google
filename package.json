{
  "name": "sign-in-with-google",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "deploy": "aws s3 cp --recursive dist s3://cloudmatica/sign-in-with-google/ --acl public-read"
  },
  "targets": {
    "default": {
      "publicUrl": "."
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jose": "^4.11.2"
  },
  "devDependencies": {
    "parcel": "^2.8.3"
  }
}
